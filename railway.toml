[build]
builder = "nixpacks"

[deploy]
startCommand = "cd taskflow && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn --bind 0.0.0.0:$PORT --workers 3 taskflow.wsgi"
healthcheckPath = "/"